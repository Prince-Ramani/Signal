import { useLocation } from "react-router-dom";

const MessageDisplayer = () => {
  const location = useLocation();
  return <div className="bg-accent-foreground"></div>;
};

export default MessageDisplayer;
